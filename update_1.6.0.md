# 업데이트 1.6.0 계획

## 목표

- 기존 TXT/EPUB에 PDF와 이미지 압축 파일을 추가한다.
- `.zip`, `.cbz`, `.7z` 안에 이미지와 다른 파일이 섞여 있어도 이미지 항목만 자연 정렬해 읽는다.
- 압축 분석과 이미지 추출은 전부 클라이언트에서 수행하고 Vercel 서버 함수는 사용하지 않는다.
- Google Drive에 사용자가 직접 올린 파일도 책장에 표시하고 최초 열기 시 검증한다.
- 일반 도서는 파일당 150MB, 한 번에 총 500MB까지 추가할 수 있게 한다.
- 압축 도서는 최대 300MB 한 개만 단독으로 추가할 수 있게 한다.

## 확정 정책

### 지원 형식

- 기존: `.txt`, `.epub`
- 추가: `.pdf`, `.zip`, `.cbz`, `.7z`
- 압축 파일 안에서는 지원 이미지 확장자만 사용한다.
- 이미지 외 TXT, EPUB, PDF, 메타데이터 및 기타 파일은 무시한다.
- 지원 이미지가 하나도 없으면 해당 압축 파일은 열 수 없는 도서로 처리한다.
- 비밀번호로 잠긴 압축 파일은 1.6.0에서 지원하지 않는다.
- SVG처럼 실행 가능한 내용을 포함할 수 있는 형식은 이미지 목록에서 제외한다.

### 업로드 용량

- TXT, EPUB, PDF는 기존 다중 선택 흐름을 유지한다.
  - 파일 하나당 최대 150MB
  - 한 번에 최대 10개
  - 선택 파일 총합 최대 500MB
- ZIP, CBZ, 7z는 최대 300MB 한 개만 선택할 수 있다.
- 압축 파일과 TXT, EPUB, PDF는 같은 선택 목록에 함께 둘 수 없다.
- 이미 일반 도서가 선택돼 있을 때 압축 파일을 추가하면 압축 파일 추가만 취소하고 기존 선택은 유지한다.
- 이미 압축 파일이 선택돼 있을 때 다른 파일을 추가하면 새 파일 추가를 취소하고 기존 압축 파일을 유지한다.
- 여러 압축 파일을 동시에 선택하면 전체 추가를 취소하고 하나만 선택하도록 안내한다.
- Google Drive에 직접 올린 파일은 책장에 표시하되 다음 제한을 넘으면 다운로드하지 않고 열기 단계에서 제한 메시지를 표시한다.
  - TXT, EPUB, PDF: 150MB
  - ZIP, CBZ, 7z: 300MB

### 압축 파일 검사 시점

압축 내부 검사를 업로드, 다운로드, 목록 표시 때마다 반복하지 않는다.

1. 앱에서 로컬 파일을 추가할 때
   - 브라우저에서 압축 엔트리 목록을 먼저 읽는다.
   - 지원 이미지가 하나 이상 있을 때만 로컬 저장과 Drive 업로드를 진행한다.
   - 전체 압축 해제는 하지 않는다.

2. Google Drive 책장을 불러올 때
   - 파일명, MIME, 크기, 수정 시각, 체크섬 같은 메타데이터만 조회한다.
   - 압축 파일은 내부 검증 전이라도 도서 후보로 책장에 표시한다.
   - 책장 로딩 중 모든 압축 파일을 다운로드하거나 내부 검사하지 않는다.

3. 사용자가 Google Drive에 직접 올린 압축 파일을 처음 열 때
   - Drive에서 원본을 브라우저로 다운로드한다.
   - 클라이언트에서 압축 엔트리 목록을 검사한다.
   - 이미지가 있으면 이미지 도서로 열고 검사 결과를 로컬에 저장한다.
   - 이미지가 없거나 암호화됐거나 손상됐으면 구체적인 오류를 표시한다.

4. 다시 열 때
   - Drive 파일 ID와 `md5Checksum`을 우선 사용하고, 체크섬이 없으면 `modifiedTime + size`를 파일 지문으로 사용한다.
   - 같은 지문의 검사 결과와 이미지 인덱스는 재사용한다.
   - 지문이 달라졌을 때만 다시 검사한다.

## 처리 구조

### 공통 도서 준비 단계

현재 모든 입력을 EPUB으로 간주하는 `ensureEpubBook` 구조를 형식별 준비 단계로 교체한다.

```ts
type ReaderFormat = 'epub' | 'pdf' | 'archive';

type PreparedBookSource = {
  book: Book;
  format: ReaderFormat;
  source: Blob | File | FoliateBook;
};
```

- EPUB은 ZIP 헤더만 보지 않고 EPUB 컨테이너 구조를 확인한다.
- TXT는 기존처럼 EPUB으로 변환한다.
- PDF는 원본 Blob/File을 PDF 어댑터에 전달한다.
- ZIP/CBZ/7z는 이미지 엔트리 인덱스를 만든 뒤 고정 레이아웃 도서 어댑터에 전달한다.
- 표시용 원본 파일명과 실제 리더에 전달할 형식 정보를 분리한다.

### 압축 처리

- ZIP/CBZ는 현재 포함된 ZIP 로더를 활용한다.
- 7z는 브라우저 Web Worker에서 실행되는 WASM 기반 압축 라이브러리를 사용한다.
- ZIP과 7z 구현 위에 공통 `ArchiveImageSource` 인터페이스를 둔다.
- 엔트리 경로는 대소문자와 유니코드를 정규화한 뒤 숫자 인식 자연 정렬한다.
- `__MACOSX`, `.DS_Store`, 숨김 파일, 디렉터리 항목은 제외한다.
- 이미지와 다른 파일이 섞여 있어도 이미지 엔트리만 페이지로 만든다.
- 모든 이미지를 한꺼번에 풀지 않고 현재 페이지와 인접 페이지를 필요할 때만 추출한다.
- 사용이 끝난 Blob URL과 페이지 캐시는 즉시 해제한다.

### PDF 처리

- Foliate의 PDF 어댑터와 고정 레이아웃 렌더러를 복원한다.
- PDF.js 본체, worker, CMap, 표준 폰트 자산을 정적 파일로 배포한다.
- PDF와 이미지 압축 도서는 고정 레이아웃 페이지 모드로 연다.
- 글꼴, 줄간격, 문단 간격 같은 EPUB 전용 설정은 고정 레이아웃 도서에 적용하지 않는다.
- PDF 검색은 실제 텍스트 레이어 검색이 검증되기 전까지 숨기거나 비활성화한다.

## Google Drive

- `web viewer` 폴더에서는 MIME만으로 제한하지 않고 파일을 조회한 뒤 지원 확장자를 클라이언트에서 필터링한다.
- Drive가 알 수 없는 형식을 `application/octet-stream`으로 기록해도 파일 확장자로 PDF/ZIP/CBZ/7z를 찾는다.
- 목록 필드에 `size`, `modifiedTime`, `md5Checksum`을 추가한다.
- 현재 OAuth 범위인 `drive.file`은 앱이 만들었거나 사용자가 앱에 명시적으로 공유한 파일 단위 접근 권한이다.
- 따라서 사용자가 Drive 웹에서 직접 올린 임의 파일이 자동으로 책장에 나타나는 동작은 `drive.file`만으로 보장하지 않는다.
- Drive 직접 업로드 파일 지원은 다음 두 방식 중 `Google Picker + drive.file`을 기본안으로 한다.
  - 권장: Google Picker에서 사용자가 파일을 한 번 선택해 앱에 접근 권한을 부여한다.
  - 비권장: 전체 Drive 접근 범위로 확대해 폴더를 자동 검색한다. 제한 범위 검증과 더 넓은 권한이 필요하므로 1.6.0 기본 범위에서 제외한다.
- TXT, EPUB, PDF는 기존 파일 선택, 원본 업로드, TXT→EPUB 로컬 변환 흐름을 유지한다.
- 다만 150MB/500MB 정책을 안정적으로 처리하기 위해 Drive 전송 방식은 기존 단일 multipart 요청 대신 resumable upload를 사용한다.
- 업로드 데이터는 Vercel을 거치지 않고 브라우저에서 Google Drive로 직접 전송한다.
- 업로드 진행률, 취소, 청크 재시도 상태를 사용자에게 표시한다.

## 개발 및 테스트 전략

로컬 Google 로그인을 전체 구현 검증의 전제 조건으로 두지 않는다.

### 구현 우선순위

코드 작성과 설계 판단은 다음 순서를 고정한다. 아래 순위는 서로 충돌할 때 상위 항목을 우선한다.

1. 목적에 부합하는가
   - 현재 Phase의 사용자 동작과 완료 조건을 직접 만족해야 한다.
   - 미래 확장, 범용화, 추상화 자체를 위해 현재 범위를 넓히지 않는다.
   - 기존 TXT/EPUB, 진행률, 북마크, 오프라인 동작을 깨뜨리지 않는다.

2. 코드가 가벼운가
   - 코드 줄 수가 아니라 CPU, 메모리, 네트워크, 저장 공간, 메인 스레드 점유가 적은지를 판단한다.
   - 150MB/300MB 원본을 불필요하게 복제하거나 전체 압축 해제하지 않는다.
   - Worker, 지연 추출, 필요한 페이지 중심 캐시, Blob URL 해제를 우선한다.
   - 동일한 Drive 파일과 압축 인덱스를 반복 다운로드·검사·변환하지 않는다.
   - 큰 라이브러리는 실제 필요한 형식에서만 동적 로드한다.

3. 코드가 우아한가
   - 형식 판별, 업로드 전송, 압축 엔트리 읽기, 이미지 도서 변환, 렌더링을 기능별 경계로 나눈다.
   - ZIP과 7z의 공통 동작은 같은 인터페이스로 묶고 형식별 차이는 어댑터에 둔다.
   - 기존 프로젝트의 hook/lib/adapter 구조를 따르고 중복 분기와 임시 우회 코드를 남기지 않는다.
   - 사용되지 않는 코드, 중복 상수, 불필요한 호환 코드와 추상화는 제거한다.

### Phase 종료 점검

각 Phase 구현이 끝나면 커밋과 테스트 전에 다음 순서로 자체 점검한다.

1. 목적 점검
   - Phase 요구사항, 완료 조건, 예외와 오류 경로를 코드와 대조한다.
   - 목적에서 벗어난 구현이나 누락된 사용자 동작이 있으면 먼저 수정한다.

2. 부하 점검
   - 대용량 파일의 메모리 복제, 전체 압축 해제, 메인 스레드 작업, 반복 네트워크 요청을 확인한다.
   - 캐시와 Blob URL의 생성·해제 수명, Worker 종료, 실패 후 자원 정리를 확인한다.
   - 더 가벼운 방식이 실제 복잡도를 크게 늘리지 않으면 그 방식으로 수정한다.

3. 구조 점검
   - 기능 경계, 이름, 중복, 불필요한 코드, 기존 구조와의 일관성을 검토한다.
   - 한 모듈이 인증, 전송, 형식 판별, 렌더링을 동시에 맡지 않게 분리한다.
   - 추상화가 실제 중복이나 복잡도를 줄이지 않으면 추가하지 않는다.

4. 점검 통과 후 검증
   - 정적 검사, 자동 테스트, 빌드를 실행한다.
   - 실패하면 수정 후 목적·부하·구조 점검부터 다시 수행한다.

5. 검증 통과 후 커밋과 배포 테스트
   - 로컬 검증이 통과한 상태만 커밋하고 푸시한다.
   - Drive 관련 Phase는 Vercel 실제 검증까지 통과해야 완료 처리한다.

### 로컬 검증

- 파일 선택 규칙, 용량 제한, 압축 검사, 이미지 정렬, PDF/압축 렌더링, IndexedDB 저장은 로그인 없이 로컬에서 검증한다.
- Drive 업로드 구현은 인증과 분리된 전송 모듈로 만든다.
- 전송 모듈은 `fetch` 구현을 주입할 수 있게 해 로컬 테스트에서 Google API를 모의 응답으로 대체한다.
- resumable upload의 다음 동작을 로컬 자동 테스트로 검증한다.
  - 업로드 세션 생성 요청
  - 청크 `Content-Range`
  - `308 Resume Incomplete`
  - 마지막 청크 완료 응답
  - 네트워크 실패 재시도
  - 취소
  - 401 세션 만료와 403 권한 오류
- 테스트용 액세스 토큰을 소스, 환경 파일, localStorage에 수동으로 복사하지 않는다.

### 실제 Google Drive 통합 검증

- 실제 로그인, 폴더 생성/조회, resumable upload, 다운로드, 삭제는 OAuth에 등록된 고정 배포 URL `https://twreader.vercel.app/`에서 검증한다.
- 매번 주소가 바뀌는 Vercel Preview URL은 Drive OAuth 통합 검증에 사용하지 않는다.
- Google OAuth의 승인된 JavaScript 원본에는 `https://twreader.vercel.app`을, 승인된 리디렉션 URI에는 `https://twreader.vercel.app/`을 등록한다.
- Firebase 로그인도 같은 고정 도메인을 Authorized domains에 등록한다.
- 각 Phase는 로컬 정적 검사와 모의 Google API 테스트를 먼저 통과한 뒤 커밋하고 푸시한다.
- Vercel 배포가 완료되면 `https://twreader.vercel.app/`에서 해당 Phase의 실제 Google Drive 동작을 검증한다.
- Chrome에 기존 로그인 세션이나 자동완성이 있으면 Google/Firebase 로그인과 OAuth 권한 동의를 직접 진행하고 검증을 이어간다.
- 새 비밀번호, 2단계 인증, 보안 확인처럼 사용자 확인이 필요한 단계에서만 사용자에게 로그인을 요청한다.
- 인증에 사용된 비밀번호, 인증 코드, 액세스 토큰은 소스, 환경 파일, 로그, 계획 문서에 기록하지 않는다.
- 실제 Drive 검증까지 통과해야 Drive 관련 Phase를 완료로 표시한다.
- 일반 통합 테스트는 작은 샘플 파일로 반복하고, 150MB/300MB 경계 파일은 릴리스 전 최종 검증에서만 사용한다.
- 테스트로 생성한 Drive 파일과 resumable upload 세션은 검증 후 정리한다.

### 선택 가능한 로컬 OAuth 설정

- 로컬 실제 로그인이 필요해지면 현재 코드가 생성하는 정확한 주소인 `http://localhost:3000/`을 OAuth 리디렉션 URI에 추가할 수 있다.
- 승인된 JavaScript 원본에는 `http://localhost:3000`을 등록한다.
- Firebase Authorized domains에는 `localhost`를 등록한다.
- 스킴, 포트, 경로와 마지막 `/`가 정확히 일치해야 한다.
- 이 설정은 선택 사항이며 1.6.0 완료 조건에는 포함하지 않는다.

## 로컬 저장과 메모리

- 150MB 일반 도서와 300MB 압축 파일을 `FileReader`로 통째로 복제하지 않고 가능한 경로에서는 `File`/`Blob`을 직접 전달한다.
- 압축 원본을 EPUB이나 CBZ로 다시 패키징하지 않는다.
- IndexedDB에는 원본 도서와 압축 이미지 인덱스만 저장하고 추출된 전체 이미지 세트는 저장하지 않는다.
- 저장 전 `navigator.storage.estimate()`로 여유 공간을 확인한다.
- 로컬 캐시 저장이 실패해도 클라우드 도서를 현재 세션에서 읽을 수 있도록 하고 오프라인 저장 실패를 별도로 알린다.
- 압축 엔트리 수, 이미지 한 장의 해제 크기, 전체 예상 해제 크기에 보호 한도를 둔다.
- 압축 폭탄, 손상된 엔트리, 지원하지 않는 압축 방식은 Worker를 종료하고 오류 처리한다.

## UI 변경

- 파일 선택 허용 목록과 안내 문구에 PDF, ZIP, CBZ, 7z를 추가한다.
- 책장과 관리 화면에서 TXT, EPUB, PDF, ZIP/CBZ, 7z 형식을 구분해 표시한다.
- 아직 검사하지 않은 Drive 압축 파일도 일반 도서처럼 표시한다.
- 압축 파일 최초 열기 중에는 `압축 파일 확인 중` 상태를 표시한다.
- 이미지가 없는 압축 파일, 암호화 파일, 손상 파일, 용량 초과를 서로 다른 메시지로 표시한다.
- 고정 레이아웃 도서에서는 의미 없는 텍스트 설정과 검색 버튼을 숨긴다.

## 구현 단계

### Phase 1: 형식 모델과 용량 정책

#### 상태

- 구현 및 로컬 검증 완료.
- PDF/ZIP/CBZ/7z 정책과 메타데이터 모델은 추가했지만, 읽기 기능이 준비되기 전까지 파일 선택 UI에서는 활성화하지 않는다.
- 현재 사용자 노출 변경은 TXT/EPUB의 파일당 150MB, 최대 10개, 총 500MB 제한이다.

#### 변경

- `Book`에 리더 형식과 Drive 파일 지문에 필요한 메타데이터를 추가한다.
- 확장자/MIME 판별을 공통 함수로 통합한다.
- TXT/EPUB/PDF에 파일당 150MB, 최대 10개, 총 500MB 제한을 적용한다.
- ZIP/CBZ/7z에 최대 300MB, 한 개 단독 선택 제한을 적용한다.
- 선택 목록에 일반 도서와 압축 파일이 함께 들어가지 않도록 양방향으로 차단한다.
- EPUB을 일반 ZIP과 구분하도록 실제 EPUB 구조를 검사한다.

#### 검증 결과

- `npm run test:formats`: 9개 테스트 통과.
- `npx tsc --noEmit`: 통과.
- `npx eslint src tests`: 통과.
- `npm run build`: 통과.
- `git diff --check`: 통과.
- 브라우저 제어 도구가 현재 세션에 노출되지 않아 로컬 모달 클릭 자동화는 수행하지 못했다.

### Phase 2: Drive 목록과 대용량 업로드

- Drive 목록 조회 필드와 클라이언트 확장자 필터를 확장한다.
- Google Picker로 공유한 Drive 직접 업로드 PDF/압축 파일이 책장에 표시되는지 확인한다.
- resumable upload, 청크 재시도, 진행률, 취소를 구현한다.
- TXT/EPUB/PDF 150MB 초과와 ZIP/CBZ/7z 300MB 초과 Drive 파일은 다운로드 전에 차단한다.

### Phase 3: ZIP/CBZ 이미지 도서

- 압축 엔트리 검사기와 공통 이미지 필터/정렬을 구현한다.
- 혼합 ZIP에서 이미지 항목만 페이지로 노출한다.
- Foliate comic-book 및 fixed-layout 모듈을 복원한다.
- 현재 페이지 중심의 지연 추출과 Blob URL 해제를 구현한다.

### Phase 4: 7z 이미지 도서

- 7z 지원 WASM과 Worker 자산을 정적 배포한다.
- ZIP과 같은 `ArchiveImageSource` 계약으로 7z 엔트리를 노출한다.
- 엔트리 목록만 먼저 읽고 이미지는 페이지 단위로 추출한다.
- Worker 실패, 암호화, 메모리 부족 오류를 사용자 메시지로 변환한다.

### Phase 5: PDF

- PDF.js와 Foliate PDF 어댑터를 추가한다.
- 페이지 이동, 진행률 저장, 목차, 재개 위치를 기존 리더와 연결한다.
- 고정 레이아웃에 맞게 설정과 검색 UI를 제한한다.
- PDF 정적 자산의 오프라인 캐시 동작을 검증한다.

### Phase 6: 검증 캐시와 안정화

- Drive 파일 지문별 압축 검사 결과와 이미지 인덱스를 저장한다.
- 수정된 Drive 파일만 재검사한다.
- IndexedDB 용량 부족 시 클라우드 전용 읽기로 폴백한다.
- 서비스워커 캐시 버전과 필요한 정적 Worker/WASM/PDF 자산 정책을 갱신한다.

## 완료 조건

- TXT/EPUB/PDF는 파일당 150MB 이하, 최대 10개, 총 500MB까지 선택할 수 있다.
- ZIP/CBZ/7z는 300MB 이하 한 개만 선택할 수 있다.
- 일반 도서와 압축 파일은 같은 선택 목록에 공존하지 않는다.
- 일반 도서 선택 후 압축 파일을 추가하면 압축 파일만 거부되고 기존 선택은 유지된다.
- 압축 파일 선택 후 다른 파일을 추가하면 새 파일만 거부되고 기존 압축 파일은 유지된다.
- 앱에서 올린 혼합 ZIP/CBZ/7z는 업로드 전에 이미지 존재 여부를 확인한다.
- Drive에 직접 올린 ZIP/CBZ/7z는 Google Picker로 앱에 공유한 뒤 책장에 표시되고 최초 열기 때 검증된다.
- 압축 안의 비이미지 파일은 무시되고 이미지 파일만 자연 순서로 표시된다.
- 책장 로딩만으로 압축 원본 다운로드나 압축 해제가 발생하지 않는다.
- 압축 해제와 PDF 렌더링에 Vercel 서버 함수가 사용되지 않는다.
- EPUB, TXT, 진행률, 북마크, 클라우드 동기화의 기존 동작이 유지된다.

## 검증

- `git diff --check`
- `npx tsc --noEmit`
- `npx eslint src`
- `npm run build`
- 로컬에서는 모의 Google API 응답으로 resumable upload 요청, 청크, 재시도, 취소, 오류 처리를 검증한다.
- 커밋과 푸시 후 `https://twreader.vercel.app/`의 Vercel 배포 완료를 확인한다.
- `https://twreader.vercel.app/`에서 실제 Drive 업로드, 다운로드, 목록 갱신, 삭제를 검증한다.
- Google Picker로 Drive 직접 업로드 파일에 접근 권한을 부여한 뒤 책장 표시와 최초 열기를 검증한다.
- 이미지 전용, 이미지와 기타 파일 혼합, 이미지 없음, 암호화, 손상 압축 파일을 검증한다.
- 파일명이 `1, 2, 10` 순서로 자연 정렬되는지 확인한다.
- ZIP/CBZ와 7z에서 페이지 이동 후 메모리가 계속 증가하지 않는지 확인한다.
- 150MB에 가까운 일반 도서와 300MB에 가까운 압축 파일의 업로드 재시도, 취소, 열기, 로컬 캐시 실패 경로를 확인한다.
- 일반 도서 여러 개가 총 500MB 경계에서 허용되고 초과 시 거부되는지 확인한다.
- 압축 파일과 다른 파일을 선택 순서를 바꿔 추가해도 단독 선택 규칙이 유지되는지 확인한다.
- iPad Safari와 데스크톱 Chromium에서 PDF 및 압축 이미지 도서를 확인한다.

## 제외 범위

- 압축 안의 TXT 또는 EPUB을 합치거나 새 EPUB으로 만드는 기능
- 서버 측 압축 해제, 변환, 썸네일 생성
- 비밀번호 입력 및 암호화 압축 해제
- RAR, TAR 및 기타 압축 형식
- OCR과 이미지 안의 텍스트 검색

## 참고

- Google Drive는 5MB를 초과하는 대용량 파일과 모바일 환경에 resumable upload 사용을 권장한다.
- 7z는 기존 JSZip/Foliate ZIP 로더로 처리할 수 없으므로 Web Worker 기반 WASM 압축 라이브러리가 필요하다.
