# update 1.5.6

## 목표

탭/페이지 전환 모드에서 마우스 휠로 이전 페이지와 다음 페이지를 이동할 수 있게 한다.

## 변경 내용

- `scroll` 모드는 기존처럼 자연 스크롤을 유지한다.
- `page`, `left-right`, `all-dir` 모드에서는 마우스 휠 입력으로 페이지를 넘긴다.
- 휠을 아래로 내리면 다음 페이지, 위로 올리면 이전 페이지로 이동한다.
- 트랙패드나 고해상도 휠의 작은 노이즈 입력은 무시한다.
- 휠 이벤트가 연속으로 들어오는 동안은 한 사이클로 보고 한 페이지만 이동한다.
- 마지막 휠 입력 후 짧은 시간 동안 추가 입력이 없을 때 다음 페이지 전환을 다시 허용한다.
- 리더 iframe 내부와 바깥 탭 오버레이 양쪽에서 같은 휠 네비게이션 로직을 사용한다.
- 설정, 테마, 목차, 검색, 북마크, 이동 입력, 리더 메뉴가 열려 있을 때는 휠 페이지 전환을 막는다.

## 검증

- `git diff --check`
- `npx tsc --noEmit`
- `npx eslint src`
- `npm run build`
- 리더의 `page`, `left-right`, `all-dir` 모드에서 휠 한 사이클당 한 페이지만 이동하는지 확인한다.
- `scroll` 모드에서 기존 스크롤 동작이 유지되는지 확인한다.
