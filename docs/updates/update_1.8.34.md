# Web Reader 1.8.34 — 메뉴 모달·모바일 바텀시트 통일

작성일: 2026-08-27

기준 커밋: `e9856cff16d351a988ab3529afbfedd915e4089c`

이전 버전: [1.8.33 EPUB 스크롤 입력 경로 최적화](./update_1.8.33.md)

상위 계획: [1.8.x 전체 계획](./update_1.8.x_plan.md)

## 목표

Shelf와 Reader 메뉴바에서 여는 모달의 정보 구조와 표면을 하나의 규칙으로 통일한다.

- 모바일에서는 전체 폭 하단 바텀시트로 표시한다.
- PC에서는 기존 중앙·상단 배치를 유지하되 모바일과 같은 테마 배경·헤더 구조를 사용한다.
- 헤더는 왼쪽 원형 닫기 버튼, 제목·선택적 보조문구, 오른쪽 기능 액션 순서로 통일한다.
- 모달별 장식 아이콘은 제거하고 새로고침·테마 추가처럼 실제 동작이 있는 액션만 남긴다.
- 번역·진행률 이동·주석 편집·설치 안내 등 메뉴바 밖의 작업 대화상자는 기존 배치를 보존한다.

## 적용 범위

상태: 완료

- Shelf: 도서 검색, 도서 추가, 정렬·필터, 라이브러리 주석, 독서 통계, 테마 설정, 오프라인 저장소.
- Reader: 본문 검색, 리더 설정, 테마 설정, 책갈피·주석, 목차, 독서 통계, 도서 정보.
- 공통 `MenuSheetHeader`와 `ReaderModalFrame.menuSheet` 계약을 추가한다.
- 640px 미만에서 하단 고정, 최대 높이 88dvh, 상단 22px 곡률과 짧은 페이드 전환을 적용한다. 기존 `animate-in` transform과 중첩되어 시트가 하단 아래로 밀리지 않도록 위치 transform은 명시적으로 제거한다.
- 모든 화면 크기에서 모달 표면은 현재 `--viewer-theme-bg`와 `--viewer-theme-text`를 명시적으로 사용한다.

## 검증 계약

- 공통 헤더의 닫기 버튼이 키보드 포커스 가능한 첫 액션으로 노출된다.
- 모바일 시트는 하단에 붙고 좌우·하단 테두리와 하단 곡률을 제거한다.
- PC 모달의 기존 최대 너비와 배치, 모달별 본문 스크롤은 유지한다.
- backdrop 클릭·Escape·포커스 복귀·body scroll lock 계약을 유지한다.
- 릴리스 버전, 서비스워커, Foliate 런타임 쿼리와 회귀 테스트 버전을 1.8.34로 일치시킨다.

## 검증 결과

상태: 완료

- `npm run build`: 통과
- `npm run check`: 통과
- `npm run test:shelf-ui`: 41건 통과. 공통 헤더·시트 DOM, 모바일 하단 부착·22px 상단 곡률·88dvh 상한 계약 포함.
- `npm run test:release`: 3건 통과. package·service worker·Foliate runtime 1.8.34 정합성 확인.
- `npm run test:browser:ci`: 정렬·필터와 도서 정보의 새 공통 헤더까지 통과한 뒤, 기존 장거리 회귀의 독서 인증 PNG clipboard 생성 대기에서 시간 초과. 이번 변경과 무관한 기존 fixture 지점이며 임시 우회 코드는 남기지 않았다.
