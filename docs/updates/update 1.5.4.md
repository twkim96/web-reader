# 업데이트 1.5.4 Phase Plan

## 목표

shelf 화면의 메뉴 dock을 스크롤 위치에 따라 상단 고정 메뉴에서 하단 중앙 dock으로 전환한다. PC에서는 도서 목록과 상단 메뉴가 닿기 시작하는 시점에 애플 dock처럼 하단 중앙으로 이동하고, 모바일에서도 스크롤 후에는 세로 메뉴 팝업 대신 동일한 기능 목록을 가진 하단 dock을 사용한다.

## 구현 상태

- Status: 구현 완료, 검증 진행.
- shelf 본문과 상단 fixed dock의 위치를 비교해 하단 dock 모드로 전환한다.
- 최소 스크롤 gate와 hysteresis를 함께 사용해 초기 진입/경계 지점 흔들림을 줄인다.
- PC 하단 dock은 같은 메뉴 목록을 하단 중앙에 표시하고, 상단 dock보다 큰 버튼 슬롯을 사용한다.
- PC 상단 dock은 로그인/로그아웃 버튼까지 함께 묶어 표시한다.
- 하단 dock과 모바일 dock은 로그인/로그아웃 버튼을 제외하고 검색, 추가, 정렬, 보기, 테마, 보관만 표시한다.
- 모바일의 로그인/로그아웃 버튼은 오른쪽 상단 헤더 위치에만 표시하고 스크롤을 따라오지 않는다.
- 로그인/로그아웃은 별도 박스 surface 없이 메뉴 dock 높이 축에 맞춘 단독 아이콘으로 표시한다.
- `LOCAL LIBRARY` 브랜드 라인은 도서 목록 컨테이너와 같은 좌우 기준을 사용하고 메뉴 dock 높이 중심선에 맞춘다.
- PC 상단 메뉴 dock은 로그인/로그아웃 아이콘을 포함해 오른쪽 끝단에 맞춘다.
- 모바일 하단 dock은 스크롤 위치와 무관하게 항상 표시한다.
- 모바일 하단 dock과 아이콘 크기를 소폭 키운다.
- 모바일 상단 메뉴 버튼과 세로 메뉴는 제거한다.
- 하단 dock은 앱 root 안에서 렌더링해 밝은 테마에서도 테마 색상/테두리 CSS 변수를 정상 상속한다.
- shelf 하단 padding을 늘려 마지막 카드가 하단 dock에 가려지지 않게 한다.
- 앱 버전과 서비스워커 캐시는 `1.5.4`로 갱신한다.

## Phase 1: 전환 기준 정의

### 구현 방향

- shelf 본문 영역의 화면상 위치와 상단 메뉴 dock의 위치를 비교한다.
- 본문이 상단 dock과 겹치기 시작하면 하단 dock 모드로 전환한다.
- 다시 충분히 위로 올라왔을 때만 상단 dock 모드로 복귀하도록 hysteresis를 둔다.

### 완료 기준

- 스크롤 경계에서 메뉴가 위/아래로 반복해서 흔들리지 않는다.
- 도서 목록이 상단 메뉴에 닿기 전에는 기존 상단 위치를 유지한다.
- 도서 목록이 상단 메뉴와 겹칠 시점에는 하단 중앙 dock으로 이동한다.

## Phase 2: PC 하단 dock

### 구현 방향

- PC 상단 dock은 기존 우측 상단 fixed 위치를 유지하고 로그인/로그아웃 아이콘을 마지막 항목으로 포함한다.
- 하단 모드에서는 같은 메뉴 목록을 하단 중앙 fixed dock으로 표시한다.
- 하단 dock은 상단 dock보다 약간 큰 버튼/아이콘 크기를 사용한다.
- 전환은 opacity와 transform 중심의 빠른 애니메이션으로 처리한다.

### 완료 기준

- PC에서 스크롤을 내리면 메뉴가 하단 중앙으로 자연스럽게 이동한 것처럼 보인다.
- 하단으로 이동한 PC dock에는 로그인/로그아웃 아이콘이 포함되지 않는다.
- 하단 dock은 화면 안에 머물고 마지막 도서 카드 조작 영역을 가리지 않는다.
- 다시 위로 올리면 메뉴가 우측 상단으로 복귀한다.

## Phase 3: 모바일 하단 dock 및 인증 버튼 분리

### 구현 방향

- 모바일 상단 메뉴 버튼은 제거한다.
- 모바일은 스크롤 위치와 무관하게 하단 dock을 항상 표시한다.
- 하단 dock은 PC와 같은 기능 목록을 모바일 폭에 맞춘 한 줄 dock으로 표시한다.
- 로그인/로그아웃 버튼은 메뉴에서 제외하고 오른쪽 상단 헤더 아이콘으로 분리한다.
- 로그인/로그아웃 버튼은 별도 rounded 박스로 감싸지 않고 아이콘 hit-area만 둔다.
- 하단 dock은 portal 없이 앱 root 안에서 렌더링해 테마 CSS 변수를 상속한다.

### 완료 기준

- 모바일에서 항상 하단 중앙에 검색, 추가, 정렬, 보기, 테마, 보관 메뉴가 한 줄로 보인다.
- 하단 dock에는 로그인/로그아웃 버튼이 포함되지 않는다.
- 오른쪽 상단의 로그인/로그아웃 버튼은 스크롤을 따라 내려오지 않는다.
- 로그인/로그아웃 아이콘은 메뉴 dock과 같은 높이 중심선에 정렬된다.
- `LOCAL LIBRARY`/와이파이 아이콘 라인은 도서 카드 영역의 좌우 끝단 기준과 맞는다.
- 도서 목록 시작 위치는 헤더와의 간격이 과하게 벌어지지 않도록 살짝 올린다.
- 밝은 테마에서도 dock 아이콘과 테두리가 충분히 보인다.
- dock은 safe-area를 고려해 하단에 배치된다.

## Phase 4: 문서/검증/배포

### 구현 방향

- `package.json`, `package-lock.json`, `public/sw.js` 버전을 `1.5.4`로 갱신한다.
- 정적 검증과 production build를 통과시킨 뒤 커밋/푸시한다.

### 자동 검증

- `git diff --check` 통과.
- `npx tsc --noEmit` 통과.
- `npx eslint src` 통과.
- `npm run build` 통과.
