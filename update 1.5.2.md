# 업데이트 1.5.2 Phase Plan

## 목표

리더의 floating 메뉴 위치를 PC/iPad에서 더 자연스럽게 맞추고, 서재 상단 메뉴를 카드형 그룹으로 정리한다. 버전과 서비스워커 캐시는 `1.5.2`로 갱신한다.

## 구현 상태

- Status: 구현 완료, 로컬 검증 완료.
- 리더 우하단 메뉴는 넓은 화면에서 `1000px + 메뉴 폭 * 2` 가상 영역의 오른쪽 끝을 기준으로 배치한다. 충분히 넓은 화면에서는 메뉴 왼쪽이 텍스트 영역 오른쪽 끝과 맞닿고, 좁은 화면에서는 safe-area 기준을 유지한다.
- 리더 상단 상시 책 제목 상태줄은 제거하고, 기존 화면 탭 시 나오는 제목 팝업만 유지한다.
- 모바일 목차 모달은 한 번에 약 8개 챕터가 보이는 높이로 줄였다.
- 서재 헤더의 하단 구분선을 제거하고 메뉴 버튼 영역을 rounded 사각형 그룹으로 묶었다.
- 모바일 서재 메뉴는 오른쪽 중앙의 세로 사이드 패널처럼 뜨도록 위치를 바꿨다.
- 서재 메뉴는 개별 버튼 박스를 겹쳐 넣는 방식 대신 Mac/iPad Dock처럼 하나의 rounded dock surface 안에 아이콘 슬롯들이 놓이는 형태로 조정했다.
- 앱 버전과 서비스워커 캐시는 `1.5.2`로 갱신했다.

## Phase 1: 리더 메뉴 위치를 텍스트 영역 바깥 경계에 맞추기

### 목적

현재 우하단 리더 메뉴는 가운데 정렬된 텍스트 영역(`maxInlineSize: 1000px`)의 오른쪽 안쪽을 기준으로 배치된다. 넓은 화면에서는 메뉴가 텍스트 끝과 떨어져 보이지 않도록, `텍스트 max width + 메뉴 폭 * 2`로 확장한 가상 영역의 오른쪽 끝에 메뉴를 맞춘다.

### 구현 방향

- `src/components/reader/ReaderToolbar.tsx`
  - 리더 텍스트 max width는 기존 Foliate layout 기준인 `1000px`를 사용한다.
  - 메뉴 폭 상수는 현재 메뉴 폭인 `18.75rem`과 일치시킨다.
  - 충분히 넓은 화면에서는 메뉴 왼쪽이 텍스트 오른쪽 경계에 붙도록 배치한다.
  - 좁은 화면에서는 기존처럼 화면 안쪽 safe-area 기준을 유지한다.

### 완료 기준

- PC 넓은 화면에서 메뉴가 본문 텍스트의 오른쪽 끝 바로 바깥에 붙는다.
- 메뉴는 화면 밖으로 나가지 않는다.
- 모바일/좁은 iPad에서는 기존 우하단 배치와 큰 차이가 없다.

## Phase 2: 상단 고정 책 제목 상태줄 제거

### 목적

상단에 항상 보이는 책 제목 상태줄은 리더 화면에서 제거한다. 기존 화면 탭 시 나오는 제목 팝업은 유지한다.

### 구현 방향

- `src/components/reader/ReaderStatusBar.tsx`
  - 상단 제목 상태줄 렌더링을 제거한다.
  - 하단 챕터명/퍼센트 상태줄은 유지한다.
- `src/components/EpubReader.tsx`
  - 상단 상태줄용 책 제목 prop 전달을 제거한다.

### 완료 기준

- 메뉴를 열지 않았을 때 리더 상단에 상시 책 제목이 보이지 않는다.
- 하단 챕터명/퍼센트 상태줄은 그대로 보인다.
- 기존 상단 제목 팝업은 그대로 유지된다.

## Phase 2-1: 모바일 목차 모달 높이 축소

### 목적

모바일 화면에서 목차 모달이 너무 길게 보이지 않도록, 한 번에 약 8개 챕터가 보이는 높이로 줄인다.

### 구현 방향

- `src/components/TocModal.tsx`
  - 모바일 기본 최대 높이를 약 8개 항목 기준으로 제한한다.
  - 작은 화면에서는 viewport 높이를 넘지 않도록 `vh` 상한도 함께 유지한다.
  - 데스크톱/넓은 화면의 기존 최대 높이는 유지한다.

### 완료 기준

- 모바일 목차 모달에서 약 8개 챕터만 한 번에 보이고 나머지는 스크롤된다.
- 데스크톱 목차 모달 높이는 기존과 크게 달라지지 않는다.

## Phase 3: 서재 헤더 메뉴를 카드형 그룹으로 재구성

### 목적

서재 화면에서 헤더와 도서 목록을 나누는 구분선을 제거하고, 메뉴 버튼들을 별도 rounded 사각형 그룹으로 묶는다. 모바일에서는 메뉴 버튼을 눌렀을 때 오른쪽 중앙에 사이드바 느낌으로 뜨게 한다.

### 구현 방향

- `src/components/shelf/ShelfHeader.tsx`
  - `header`의 `border-b`를 제거한다.
  - 데스크톱 메뉴 버튼 묶음을 Mac/iPad Dock 같은 단일 rounded surface로 감싼다.
  - 내부 아이콘 버튼은 개별 border/card를 제거하고 dock 안의 icon slot처럼 보이게 한다.
  - 모바일 메뉴 패널은 드롭다운 대신 `fixed inset-y-0 right-* flex items-center` 사이드 dock으로 배치한다.
  - 검색 버튼과 모바일 메뉴 버튼의 시각 그룹감도 유지한다.

### 완료 기준

- 서재 헤더 하단 구분선이 사라진다.
- 데스크톱 메뉴 버튼들이 단일 dock surface 안에 묶여 보인다.
- 모바일 메뉴는 화면 오른쪽 중앙에 세로 dock처럼 뜬다.

## Phase 4: 버전/캐시 및 검증

### 구현 방향

- `package.json`과 `package-lock.json` 버전을 `1.5.2`로 올린다.
- `public/sw.js` 캐시 이름을 `pc-reader-v1.5.2`로 올린다.

### 자동 검증

- `npx tsc --noEmit` 통과.
- `npx eslint src` 통과.
- `npm run build` 통과.
- `git diff --check` 통과.

### 수동 검증

- PC 넓은 화면에서 리더 메뉴가 텍스트 영역 오른쪽 바깥에 붙는지 확인한다.
- 모바일/좁은 iPad에서 메뉴가 화면 밖으로 나가지 않는지 확인한다.
- 리더 상단에 상시 책 제목이 사라지고 기존 팝업은 유지되는지 확인한다.
- 모바일 목차 모달이 약 8개 챕터 높이로 제한되는지 확인한다.
- 서재 데스크톱 헤더에서 구분선 없이 메뉴 그룹이 카드형으로 보이는지 확인한다.
- 모바일 서재 메뉴가 오른쪽 중앙 사이드바 형태로 열리는지 확인한다.
