# update 1.5.4

## 목표

shelf의 도서 추가 모달을 드래그앤드롭 중심으로 개선한다. 파일 시스템을 직접 뒤지지 않아도 모달의 + 드롭존에 파일을 놓으면 바로 추가되고, 기존 파일 선택 방식은 별도의 업로드 버튼으로 유지한다.

## Phase 1: 업로드 처리 공통화

- hidden file input의 단일 파일 처리 로직을 `FileList | File[]` 공통 처리 함수로 확장한다.
- 드롭 파일과 파일 선택 결과가 같은 저장/동기화 경로를 사용하게 한다.
- 한 번에 추가 가능한 도서는 최대 10개로 제한한다.
- `.txt`, `.epub`만 처리하고 그 외 파일은 안내한다.

## Phase 2: import 모달 UI

- 기존 확인 모달 대신 shelf 전용 import 모달을 사용한다.
- 상단 안내 아이콘 아래에 큰 + 드롭존을 배치한다.
- 드롭존은 drag hover 상태를 시각적으로 표시한다.
- 기존 파일 선택 방식은 `업로드` 버튼으로 제공한다.
- 오프라인/게스트 안내 문구와 클라우드 로그인/연결 동선은 유지한다.

## Phase 3: 검증

- `git diff --check`
- `npx tsc --noEmit`
- `npx eslint src`
- `npm run build`
- 브라우저에서 도서 추가 모달, 드롭존, 업로드 버튼 노출을 확인한다.
