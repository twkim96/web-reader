# update 1.5.7

## 목표

shelf 리스트 모드를 카드형 UI가 아니라 공간 효율이 높은 정보 행 UI로 변경한다.

## 변경 내용

- 리스트 모드에서 각 도서의 큰 카드 배경, 둥근 박스, 외곽 테두리를 제거한다.
- Apple Music 리스트처럼 도서 정보를 한 줄 행 중심으로 나열한다.
- 행 높이와 상하 패딩을 줄여 한 화면에 더 많은 도서가 보이게 한다.
- 도서 아이콘, 제목, 읽은 날짜, 문서 타입, 진행률을 컬럼 형태로 정리한다.
- 모바일에서는 문서 타입 컬럼을 숨겨 좁은 화면에서도 제목과 진행률이 우선 보이게 한다.
- 리스트 모드의 부모 `gap`을 제거하고 행 구분선 기준으로 간격을 정리한다.
- 그리드 모드 카드 UI는 기존 형태를 유지한다.

## 검증

- `git diff --check`
- `npx tsc --noEmit`
- `npx eslint src`
- `npm run build`
- shelf 리스트 모드에서 카드형 박스가 사라지고 정보 행으로 촘촘하게 표시되는지 확인한다.
- shelf 그리드 모드가 기존 카드 UI를 유지하는지 확인한다.
